import React from "react";

import PropTypes from "prop-types";

export default function CashflowerTab() {
  return <section>Cashflower</section>;
}

CashflowerTab.propTypes = {
  // show: PropTypes.bool.isRequired,
};
